# בניית עמודי המאמרים

## מתי להריץ
אחרי כל **הוספה, עריכה או מחיקה** של מאמר בפאנל `/crm`.
בלי זה העמוד הציבורי ימשיך להציג את הגרסה הקודמת.

## איך
מהתיקייה `~/Desktop/business-website`:

```
python3 tools/build_articles.py
git add -A && git commit -m "Rebuild article pages" && git push
```

זהו. הסקריפט קורא את המאמרים מ-Firestore ובונה מחדש את כל העמודים.

## למה זה נחוץ
GitHub Pages מגיש קבצים בלבד — הוא לא מייצר עמודים בזמן אמת.
קודם לכן כל המאמרים הוגשו מקובץ אחד (`/article/?id=…`) עם אותה כותרת
ואותו `canonical`, וגוגל סימנה אותם *Alternate page with proper canonical
tag* ולא אינדקסה אף אחד. עכשיו לכל מאמר יש עמוד משלו.

## פרטים
- כתובות: `/article/<slug>/` ו-`/en/article/<slug>/`
- ה-slug נקבע פעם אחת לכל מאמר ונשמר ב-`data/article-slugs.json`,
  לפי מזהה המסמך ב-Firestore — כך שינוי כותרת לא שובר כתובת קיימת.
- מאמר חדש מקבל slug אוטומטית מהכותרת באנגלית.
- `sitemap.xml` מתעדכן על ידי הסקריפט.
- הכתובות הישנות `?id=` ממשיכות לעבוד ומפנות לעמוד הסטטי.
