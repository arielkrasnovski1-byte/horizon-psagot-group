# פאנל ניהול עסקאות — מדריך הפעלה

נבנה סקשן **"עסקאות לדוגמה"** + פאנל ניהול (Decap CMS) שמאפשר להתחבר, להוסיף/לערוך/למחוק עסקאות ולהעלות תמונות — **בלי Firebase**. התוכן נשמר כקובץ בתוך ה-repo של האתר.

---

## מה כבר עובד עכשיו (גם על GitHub Pages)

- **עמוד עסקאות בעברית:** `/deals/`
- **עמוד עסקאות באנגלית:** `/en/deals/`
- קישור "עסקאות" נוסף לתפריט בכל עמודי האתר (עברית + אנגלית)
- הכרטיסים נטענים אוטומטית מהקובץ `data/deals.json` — בסגנון העיצוב הקיים של האתר

> העמוד יציג את העסקאות ברגע שהקובץ `data/deals.json` יפורסם. יש בו כרגע 2 עסקאות לדוגמה — אפשר למחוק/לשנות אותן דרך הפאנל.

---

## מה צריך פעם אחת כדי שהפאנל (`/admin`) יעבוד

הפאנל משתמש ב-**Netlify Identity** להתחברות וב-**Git Gateway** לשמירה. לכן צריך פעם אחת להעביר את אירוח האתר ל-Netlify (חינם). זה גם משאיר את הקוד ב-GitHub כפי שהוא.

### שלב 1 — חיבור האתר ל-Netlify
1. להיכנס ל-<https://app.netlify.com> ולהתחבר עם חשבון GitHub.
2. **Add new site → Import an existing project → GitHub** ולבחור את ה-repo של האתר.
3. Build command: להשאיר ריק. Publish directory: `/` (שורש). **Deploy**.
4. האתר יעלה לכתובת זמנית כמו `random-name.netlify.app`. אפשר בהמשך לחבר את הדומיין `horizonpsagotgroup.com` דרך **Domain settings** (מעדכנים DNS — אני אלווה בזה).

### שלב 2 — הפעלת Identity + Git Gateway
1. ב-Netlify: **Site configuration → Identity → Enable Identity**.
2. תחת **Identity → Registration** לבחור **Invite only** (חשוב — כך רק מי שמוזמן יכול להיכנס).
3. תחת **Identity → Services → Git Gateway → Enable Git Gateway**.

### שלב 3 — הזמנת המשתמש היחיד
1. **Identity → Invite users** והזנת האימייל של האדם שינהל את העסקאות (למשל `office@horizon-psagot-group.com`).
2. יגיע מייל הזמנה → לוחצים על הקישור → נפתח האתר → מגדירים סיסמה → מועברים אוטומטית ל-`/admin`.
3. מאותו רגע נכנסים תמיד דרך **`horizonpsagotgroup.com/admin`** עם האימייל והסיסמה.

> להוספת/הסרת גישה בעתיד: Netlify → Identity → Invite/Delete user. כרגע מוגדר משתמש אחד בלבד, כפי שביקשת.

---

## איך מוסיפים עסקה (אחרי ההקמה)

1. להיכנס ל-`horizonpsagotgroup.com/admin` ולהתחבר.
2. **תוכן האתר → עסקאות לדוגמה**.
3. **Add עסקה**, למלא: כותרת (עברית/אנגלית), תיאור, קטגוריה, מיקום, היקף/סכום, שנה, ולגרור תמונה.
4. **Publish** — העסקה עולה לאתר תוך דקה (Netlify בונה מחדש אוטומטית).

- שינוי סדר העסקאות: גרירה של הפריטים ברשימה.
- מחיקה/עריכה: פתיחת העסקה ברשימה.
- שני שדות שפה בכל עסקה = ניהול עברית ואנגלית ממקום אחד.

---

## קבצים שנוספו/שונו

| קובץ | תפקיד |
|------|--------|
| `deals/index.html` | עמוד עסקאות (עברית) |
| `en/deals/index.html` | עמוד עסקאות (אנגלית) |
| `data/deals.json` | מאגר העסקאות (מה שהפאנל עורך) |
| `js/deals.js` | טוען ומצייר את הכרטיסים |
| `css/components.css` | נוספו סגנונות `.deal-card` / `.deal-facts` |
| `admin/index.html` + `admin/config.yml` | פאנל Decap CMS |
| `index.html` | נוסף חיבור Netlify Identity (הפניה לפאנל) |
| כל עמודי האתר | נוסף קישור "עסקאות" בתפריט |
| `sitemap.xml`, `robots.txt` | עודכנו (עמוד חדש + חסימת `/admin` מאינדוקס) |

---

## חלופה בלי מעבר ל-Netlify (למקרה הצורך)

אם מעדיפים להישאר על GitHub Pages בלבד, אפשר להגדיר את Decap עם `backend: github` + שרת OAuth חיצוני קטן. זה עובד, אבל דורש הקמת אפליקציית OAuth ב-GitHub ופרוקסי — יותר מורכב מ-Netlify. ההמלצה: Netlify.
